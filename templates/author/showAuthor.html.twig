<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{{ author.username }}</title>
    <style>
        .author-details {
            text-align: center;
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            max-width: 400px;
            margin: 0 auto;
        }

        .author-details img {
            border-radius: 50%;
            width: 200px;
            height: 200px;
            object-fit: cover;
        }

        .author-details h2 {
            text-transform: uppercase;
            font-size: 1.5em;
            color: #333;
        }

        .author-details p {
            font-size: 1.1em;
            color: #555;
        }
    </style>
</head>
<body>
    <div class="author-details">
        <img src="{{ asset(author.picture) }}" alt="{{ author.username }}">
        <h2>{{ author.username }}</h2>
        <p>Email: {{ author.email }}</p>
        <p>Number of Books: <strong>{{ author.nb_books }}</strong></p>
    </div>
</body>
</html>
